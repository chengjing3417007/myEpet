/*  export default {
  filterPinpaiList (state) {
    const pinpaiList = []
    const {categoryList} = state
    categoryList.forEach((item, index) => {
      if (item.cate_list.length>1) {

      }
    })
    return pinpaiList
  }
} */
